# CTF Command Center — Server & Bot Requirements
# Install with: pip install -r requirements-server.txt

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9    # file upload support

# HTTP client (for bot → server calls)
httpx>=0.27.0

# Telegram bot (Singapore team)
python-telegram-bot>=21.0

# Already in requirements.txt — listed here for standalone install
python-dotenv>=1.0.0
pyyaml>=6.0
